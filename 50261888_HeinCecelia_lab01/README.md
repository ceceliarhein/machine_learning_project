#Lab 01
Name: Cecelia Hein 
Student ID: 50261888

This project performs exploratory data analysis on a cafe sales dataset.
The goal of this lab is to clean the dataset, analyze missing values, detect outliers, and calculate statistics grouped by category.

The following functions were completed:
1. summarize_missing()
2. clean_data()
Performs data cleaning
3. detect_outliers_iqr()
Detects outliers using the Interquartile Range (IQR) method.
4. compute_group_stats()
Calculates statistics grouped by category

Install required packages:
```bash
pip install -r requirements.txt
python src/lab01.py
